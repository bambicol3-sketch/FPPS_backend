# 저장소에 requirements.txt 가 존재하지 않아(과거 커밋에서 삭제된 채 복원되지
# 않은 상태) app/ 전체의 실제 import 문을 스캔해 재구성했습니다. 팀이 실제
# 테스트한 정확한 버전 고정값은 아니므로, 배포 전 `pip freeze` 로 검증 후
# 정확한 버전으로 고정하는 것을 권장합니다.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12

sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
pgvector>=0.3
redis>=5.0

openai>=1.54
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2

httpx>=0.27
beautifulsoup4>=4.12
trafilatura>=1.12
youtube-transcript-api>=0.6
kiwipiepy>=0.18

python-pptx>=1.0
pypdf>=5.0

APScheduler>=3.10
